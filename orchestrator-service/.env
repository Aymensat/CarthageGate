# Orchestrator-Service Environment Variables

# --- Service Endpoints ---
# These variables define the connection details for the microservices
# that this orchestrator calls. When using Docker Compose, the hosts
# should be the service names defined in the docker-compose.yml file.

# SOAP Air Quality Service
AIR_QUALITY_SERVICE_HOST=localhost
AIR_QUALITY_SERVICE_PORT=8081

# REST Mobility Service
MOBILITY_SERVICE_HOST=localhost
MOBILITY_SERVICE_PORT=8080

# gRPC Emergency Service
EMERGENCY_SERVICE_HOST=localhost
EMERGENCY_SERVICE_PORT=5001
